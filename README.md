To get the service registered:
1) clone https://github.com/omaksymenko/eureka-server
2) start eureka-server application
3) open http://localhost:7171 and see my-client(the app name from properties) registered
